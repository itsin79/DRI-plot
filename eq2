import math

# تعریف دماها (مثال) - شما باید مقدارهای واقعی را وارد کنید
T_R = 1000  # دمای واکنش‌دهنده‌ها
T_F = 1200  # دمای محصولات

# محاسبه لگاریتم بر اساس فرمول داده شده
log_K_eq_1 = 1.66 + 1890 / T_R
log_K_eq_2 = -1.66 + 1890 / T_F

# تبدیل لگاریتم به مقدار K
K_eq_1 = math.exp(log_K_eq_1 * math.log(10))  # تبدیل از log به مقدار K
K_eq_2 = math.exp(log_K_eq_2 * math.log(10))  # تبدیل از log به مقدار K

# نمایش نتایج
print("K_eq_1:", K_eq_1)
print("K_eq_2:", K_eq_2)
